import { FormatCreaditCardPipe } from './format-creadit-card.pipe';

describe('FormatCreaditCardPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCreaditCardPipe();
    expect(pipe).toBeTruthy();
  });
});
