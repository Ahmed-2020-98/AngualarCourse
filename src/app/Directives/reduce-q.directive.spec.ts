import { ReduceQDirective } from './reduce-q.directive';

describe('ReduceQDirective', () => {
  it('should create an instance', () => {
    const directive = new ReduceQDirective();
    expect(directive).toBeTruthy();
  });
});
